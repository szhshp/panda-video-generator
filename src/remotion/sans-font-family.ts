/**
 * System sans stack so headless render works without fetching Inter from Google Fonts.
 */
export const REMOTION_SANS_FONT_FAMILY =
  'ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif';
